# F18雷达系统管理员手册

## 目录
1. [系统概述](#系统概述)
2. [环境依赖](#环境依赖)
3. [安装配置](#安装配置)
4. [服务启动](#服务启动)
5. [AI智能体配置](#ai智能体配置)
6. [数据统计](#数据统计)
7. [数据库说明](#数据库说明)
8. [故障排除](#故障排除)

## 系统概述

F18雷达系统是一个基于Web的雷达操作模拟器，包含以下主要组件：
- **前端**: React + TypeScript + Vite
- **后端**: Python + WebSocket
- **数据库**: SQLite
- **AI智能体**: 可配置的智能辅助系统

## 环境依赖

### 前端依赖
- Node.js 18+ 
- pnpm (推荐) 或 npm

### 后端依赖
- Python 3.8+
- 依赖包列表见 `server/requirements.txt`

## 安装配置

### 1. 克隆项目
```bash
git clone <repository-url>
cd F18-Radar-main
```

### 2. 安装前端依赖
```bash
# 使用 pnpm (推荐)
pnpm install

# 或使用 npm
npm install
```

### 3. 安装后端依赖
```bash
cd server
pip install -r requirements.txt
```

### 4. 创建必要目录
```bash
# 确保数据目录存在
mkdir -p server/data
```

## 服务启动

### 开发环境启动

#### 1. 启动后端服务
```bash
cd server
python main.py
```
或使用启动脚本：
```bash
cd server
python start.py
```

**预期输出**：
```
✅ [时间] main: 雷达系统服务器 v2.0 - 模块化架构
✅ [时间] main: ==================================================
✅ [时间] main: === 雷达系统初始化 ===
✅ [时间] main: 1. 初始化数据库...
✅ [时间] database: 数据库初始化成功
✅ [时间] main: 2. 启动WebSocket服务器...
✅ [时间] websocket: WebSocket服务器启动成功，监听端口: 8765
✅ [时间] main: 3. 启动任务管理器...
✅ [时间] task: 任务管理器启动成功
✅ [时间] main: 4. 启动威胁管理器...
✅ [时间] threat: 威胁管理器启动成功
✅ [时间] main: 5. 启动目标管理器...
✅ [时间] target: 目标管理器启动成功
✅ [时间] main: 6. 启动AI助手...
✅ [时间] ai: AI助手启动成功
✅ [时间] main: === 雷达系统启动完成 ===
```

#### 2. 启动前端服务
```bash
# 在项目根目录
pnpm dev
```
或
```bash
npm run dev
```

**预期输出**：
```
  VITE v4.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

### 生产环境部署

#### 1. 构建前端
```bash
pnpm build
```

#### 2. 配置Web服务器
将 `dist` 目录部署到Web服务器（如Nginx、Apache）

#### 3. 启动后端服务
```bash
cd server
python main.py
```

## AI智能体配置

### 配置文件位置
`public/agent_level.json`

### 配置结构说明

```json
{
  "current_level": "L2",                    // 当前默认AI等级
  "levels": [                               // AI等级配置数组
    {
      "level": "L0",                        // 等级标识
      "desc": "低级智能体，延时最长，自动处理最慢",
      "threat_select_delay_ms": 1000,       // 威胁选择延时(毫秒)
      "tdc_select_delay_ms": 1000,          // TDC选择延时(毫秒)
      "decision_probabilities": [0.3, 0.5]  // 决策概率数组
    }
  ],
  "game_settings": {                        // 游戏设置
    "current_difficulty": "high",           // 当前难度
    "audio_enabled": true,                  // 音频开关
    "max_repetitions": 10,                  // 最大重复次数
    "practice_repetitions": 1,              // 练习重复次数
    "difficulty_levels": {                  // 难度等级配置
      "high": {
        "name": "高",
        "threat_count": 10,                 // 威胁数量
        "target_count": 10                  // 目标数量
      }
    },
    "execution_order": {                    // 执行顺序配置
      "difficulty_order": ["high", "low"],  // 难度执行顺序
      "level_order": ["L0", "L1", "L2"],   // AI等级执行顺序
      "audio_options": [true, false]        // 音频选项
    }
  }
}
```

### 配置参数详解

#### AI等级配置 (levels)
- **level**: AI等级标识 (L0, L1, L2)
- **desc**: 等级描述
- **threat_select_delay_ms**: 威胁选择延时，影响AI响应速度
- **tdc_select_delay_ms**: TDC选择延时，影响AI操作速度
- **decision_probabilities**: 决策概率，控制AI的准确性

#### 游戏设置 (game_settings)
- **current_difficulty**: 当前难度级别
- **audio_enabled**: 是否启用音频
- **max_repetitions**: 正式任务最大重复次数
- **practice_repetitions**: 练习模式重复次数

#### 难度等级 (difficulty_levels)
- **threat_count**: 威胁目标数量
- **target_count**: 雷达目标数量

### 配置修改步骤

1. **编辑配置文件**
```bash
# 使用文本编辑器
notepad public/agent_level.json
# 或
code public/agent_level.json
```

2. **修改参数**
   - 调整AI等级延时
   - 修改决策概率
   - 更改难度设置
   - 调整执行顺序

3. **重启服务**
```bash
# 停止当前服务 (Ctrl+C)
# 重新启动
cd server
python main.py
```

## 数据统计

### 统计脚本位置
`server/data_statistics.py`

### 生成统计数据

#### 1. 运行统计脚本
```bash
cd server
python data_statistics.py
```

#### 2. 预期输出
```
正在清空现有统计数据...
现有统计数据已清空
正在生成传感器任务统计数据...
生成了 X 条传感器任务统计数据
正在生成威胁排序任务统计数据...
生成了 Y 条威胁任务统计数据
正在保存统计数据到数据库...
已保存 X 条传感器任务统计数据和 Y 条威胁任务统计数据
数据统计完成！
```

### 统计结果表

#### 传感器任务统计表 (sensor_task_statistics)
包含字段：
- user_id: 用户名
- task_id: 任务ID
- is_ai_active: 是否启用AI
- ai_level: AI等级
- difficulty_level: 难度等级
- audio_enabled: 音频开关
- repetition_count: 重复序号
- radar_settings_time: 雷达设置时间
- antenna_settings_time: 天线设置时间
- target_selection_time: 目标选择时间
- total_task_time: 总任务时间
- is_correct: 是否正确
- selection_method: 选择方式
- task_settings_detail: 任务设置详情

#### 威胁任务统计表 (threat_task_statistics)
包含字段：
- user_id: 用户名
- task_id: 任务ID
- is_ai_active: 是否启用AI
- ai_level: AI等级
- difficulty_level: 难度等级
- audio_enabled: 音频开关
- repetition_count: 重复序号
- threat_selection_time: 威胁选择时间
- total_task_time: 总任务时间
- is_correct: 是否正确
- selection_method: 选择方式
- task_settings_detail: 任务设置详情

### 查看统计数据

#### 使用SQLite工具
```bash
# 安装SQLite工具 (Windows)
# 下载 SQLite Studio 或使用命令行

# 连接数据库
sqlite3 server/data/radar_operations.db

# 查看传感器任务统计
SELECT * FROM sensor_task_statistics LIMIT 10;

# 查看威胁任务统计
SELECT * FROM threat_task_statistics LIMIT 10;

# 查看统计摘要
SELECT 
    COUNT(*) as total_tasks,
    AVG(total_task_time) as avg_task_time,
    AVG(CASE WHEN is_correct = 'true' THEN 1 ELSE 0 END) as accuracy_rate
FROM sensor_task_statistics;
```

## 数据库说明

### 数据库文件位置
`server/data/radar_operations.db`

### 数据库表结构

#### 1. user_operations (用户操作表)
```sql
CREATE TABLE user_operations (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    task_id INTEGER,
    operation_type TEXT NOT NULL,
    timestamp INTEGER NOT NULL,
    receive_timestamp INTEGER,
    is_active INTEGER NOT NULL,
    parameters TEXT,
    user_id TEXT,
    event_owner TEXT,
    is_correct TEXT DEFAULT 'not_set',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 2. task_settings (任务设置表)
```sql
CREATE TABLE task_settings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    task_id INTEGER NOT NULL UNIQUE,
    user_id TEXT,
    event_owner TEXT,
    execution_timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    repetition_count INTEGER NOT NULL,
    is_ai_active BOOLEAN NOT NULL,
    ai_level_config TEXT,
    difficulty_config TEXT NOT NULL,
    audio_enabled BOOLEAN NOT NULL,
    ai_level_name TEXT,
    difficulty_name TEXT NOT NULL
);
```

#### 3. user_progress (用户进度表)
```sql
CREATE TABLE user_progress (
    user_id TEXT NOT NULL,
    task_type TEXT NOT NULL,
    current_scenario_json TEXT,
    repetition_counter INTEGER,
    ai_queue_json TEXT,
    manual_queue_json TEXT,
    is_completed BOOLEAN DEFAULT FALSE,
    last_updated TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (user_id, task_type)
);
```

**重要字段说明**：
- `is_completed`: 任务完成状态
  - `TRUE`: 任务进行中/活跃状态 - 生成新任务时设置
  - `FALSE`: 任务已完成/非活跃状态 - 任务结束时设置

#### 4. sensor_task_statistics (传感器任务统计表)
```sql
CREATE TABLE sensor_task_statistics (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id TEXT,
    task_id INTEGER,
    is_ai_active BOOLEAN,
    ai_level TEXT,
    difficulty_level TEXT,
    audio_enabled BOOLEAN,
    repetition_count INTEGER,
    radar_settings_time INTEGER,
    antenna_settings_time INTEGER,
    target_selection_time INTEGER,
    total_task_time INTEGER,
    is_correct TEXT,
    selection_method TEXT,
    task_settings_detail TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 5. threat_task_statistics (威胁任务统计表)
```sql
CREATE TABLE threat_task_statistics (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id TEXT,
    task_id INTEGER,
    is_ai_active BOOLEAN,
    ai_level TEXT,
    difficulty_level TEXT,
    audio_enabled BOOLEAN,
    repetition_count INTEGER,
    threat_selection_time INTEGER,
    total_task_time INTEGER,
    is_correct TEXT,
    selection_method TEXT,
    task_settings_detail TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 数据库维护

#### 备份数据库
```bash
# 复制数据库文件
cp server/data/radar_operations.db server/data/radar_operations_backup_$(date +%Y%m%d_%H%M%S).db
```

#### 清理旧数据
```sql
-- 删除30天前的操作记录
DELETE FROM user_operations 
WHERE created_at < datetime('now', '-30 days');

-- 删除30天前的任务设置
DELETE FROM task_settings 
WHERE execution_timestamp < datetime('now', '-30 days');
```

#### 优化数据库
```sql
-- 重建索引
REINDEX;

-- 清理碎片
VACUUM;

-- 分析表统计信息
ANALYZE;
```

## 故障排除

### 常见问题

#### 1. 数据库连接错误
**错误信息**: `unable to open database file`

**解决方案**:
```bash
# 检查目录权限
ls -la server/data/

# 创建数据目录
mkdir -p server/data

# 检查磁盘空间
df -h
```

#### 2. 端口占用错误
**错误信息**: `Address already in use`

**解决方案**:
```bash
# 查找占用端口的进程
netstat -ano | findstr :8765

# 终止进程
taskkill /PID <进程ID> /F
```

#### 3. 依赖包缺失
**错误信息**: `ModuleNotFoundError`

**解决方案**:
```bash
# 重新安装依赖
cd server
pip install -r requirements.txt

# 或使用虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
pip install -r requirements.txt
```

#### 4. 前端构建失败
**错误信息**: 构建错误

**解决方案**:
```bash
# 清理缓存
pnpm clean
rm -rf node_modules
pnpm install

# 重新构建
pnpm build
```

### 日志查看

#### 后端日志
后端服务会在控制台输出详细日志，包括：
- 系统启动信息
- 数据库操作日志
- WebSocket连接日志
- 错误和异常信息

#### 前端日志
在浏览器开发者工具中查看：
- Console: JavaScript错误和日志
- Network: 网络请求状态
- Application: 本地存储和缓存

### 性能监控

#### 数据库性能
```sql
-- 查看表大小
SELECT 
    name,
    sqlite_compileoption_used('ENABLE_DBSTAT_VTAB') as dbstat_enabled,
    page_count * page_size as size_bytes
FROM pragma_page_count(), pragma_page_size()
WHERE name = 'user_operations';

-- 查看查询性能
EXPLAIN QUERY PLAN SELECT * FROM user_operations WHERE task_id = 1;
```

#### 系统资源
```bash
# 查看内存使用
top -p $(pgrep python)

# 查看磁盘使用
du -sh server/data/

# 查看网络连接
netstat -an | grep 8765
```

## 联系支持

如遇到技术问题，请提供以下信息：
1. 错误日志
2. 系统环境信息
3. 复现步骤
4. 配置文件内容

---

**文档版本**: v1.0  
**最后更新**: 2025年7月  
**维护者**: 系统管理员 