# F18-Radar 项目结构分析文档

## 一、项目概述

F18-Radar 是一个基于 Web 的 **F18-C 雷达操作模拟训练系统**，用于训练和评估雷达操作技能。系统包含**雷达目标识别**和**SA（态势感知）威胁应对**两个核心任务模块，支持 AI 辅助操作、难度动态调整、操作记录与数据分析等功能。

---

## 二、技术栈总览

| 层级 | 技术 | 说明 |
|------|------|------|
| **前端框架** | React 18.3 + TypeScript | SPA 单页应用 |
| **构建工具** | Vite 5.4 | 开发服务器 + 生产构建 |
| **图形渲染** | React-Konva + Konva | 2D Canvas 雷达界面渲染 |
| **状态管理** | MobX 6.13 | 响应式状态管理 |
| **样式方案** | Tailwind CSS 3.4 | 深色军事风格 UI |
| **后端框架** | Python + aiohttp | 异步 HTTP/WebSocket 服务器 |
| **数据库** | SQLite（WAL 模式） | 操作记录与进度持久化 |
| **通信协议** | WebSocket | 实时双向通信 |
| **数值计算** | NumPy | 目标/威胁位置计算 |

---

## 三、项目目录总览

```
F18-Radar-main/
├── src/                        # 📱 前端源代码（React + TypeScript）
│   ├── components/             #   UI 组件（29个）
│   ├── stores/                 #   MobX 状态管理
│   ├── hooks/                  #   自定义 React Hooks
│   ├── utils/                  #   工具函数
│   ├── managers/               #   管理器（音频等）
│   ├── icons/                  #   SVG 图标组件
│   ├── data/                   #   模拟数据
│   ├── main.tsx                #   应用入口
│   ├── App.tsx                 #   主应用组件
│   └── index.css               #   全局样式
│
├── server/                     # 🖥️ 后端代码（Python + aiohttp）
│   ├── core/                   #   核心业务逻辑
│   ├── managers/               #   功能管理器
│   ├── network/                #   网络通信层
│   ├── models/                 #   数据模型
│   ├── services/               #   业务服务
│   ├── data/                   #   数据库文件
│   ├── tests/                  #   测试文件
│   ├── docs/                   #   项目文档
│   ├── legacy/                 #   旧版备份
│   ├── main.py                 #   后端入口
│   ├── start.py                #   启动脚本
│   └── requirements.txt        #   Python 依赖
│
├── public/                     # 📁 静态资源
│   └── agent_level.json        #   AI 智能体 & 难度配置
│
├── dist/                       # 📦 构建输出目录
├── package.json                # 前端依赖配置
├── vite.config.ts              # Vite 构建配置
├── tsconfig.json               # TypeScript 配置
├── tailwind.config.js          # Tailwind 配置
└── 管理员手册.md                # 系统管理文档
```

---

## 四、前端代码结构（src/）

### 4.1 目录结构

```
src/
├── main.tsx                          # 应用入口文件
├── App.tsx                           # 主应用组件（视图切换、WebSocket管理）
├── index.css                         # 全局样式
│
├── components/                       # UI 组件
│   ├── Radar.tsx                     # 🎯 雷达主组件（核心控制器）
│   ├── RadarDisplay.tsx              # 雷达显示组件（Konva Canvas 渲染）
│   ├── RadarButtons.tsx              # 雷达控制按钮面板
│   ├── RadarRenderers.tsx            # 雷达渲染器（网格、刻度等）
│   ├── SAPage.tsx                    # SA 态势感知页面
│   ├── SAButtons.tsx                 # SA 页面按钮
│   ├── ThreatList.tsx                # 威胁列表组件
│   ├── ThreatAssistant.tsx           # 威胁助手组件
│   ├── AIAssistant.tsx               # AI 助手面板
│   ├── AIChat.tsx                    # AI 聊天对话组件
│   ├── CommunicationLog.tsx          # 通信日志组件
│   ├── TaskInfoDisplay.tsx           # 任务信息显示
│   ├── InitialFormModal.tsx          # 初始表单弹窗（用户信息输入）
│   ├── CompletionModal.tsx           # 任务完成弹窗
│   ├── DifficultyChangeModal.tsx     # 难度变化提示弹窗
│   ├── ScenarioCompletionModal.tsx   # 场景完成弹窗
│   ├── UnknownTarget.tsx             # 未知目标组件
│   ├── UnknownTargetManager.tsx      # 未知目标管理器
│   ├── EnemyTarget.tsx               # 敌方目标组件
│   ├── TDC.tsx                       # 目标指定光标（Target Designation Cursor）
│   ├── ScanLine.tsx                  # 扫描线动画
│   ├── BScan.tsx                     # B 扫描显示
│   ├── HorizonHUD.tsx                # 地平线 HUD 抬头显示
│   ├── NavigationHUD.tsx             # 导航 HUD
│   ├── VelocityVector.tsx            # 速度矢量指示
│   ├── VerticalText.tsx              # 垂直文本渲染
│   ├── AntennaElevationControl.tsx   # 天线仰角控制滑块
│   ├── AntennaElevationMarker.tsx    # 天线仰角标记
│   └── ConnectionStatus.tsx          # WebSocket 连接状态指示
│
├── stores/                           # MobX 状态管理
│   ├── RadarStore.ts                 # 雷达状态（系统参数、目标锁定、任务进度）
│   ├── AgentStore.ts                 # AI 代理状态（AI等级、操作归属）
│   └── StoreProvider.tsx             # Store 全局提供者
│
├── hooks/                            # 自定义 Hooks
│   ├── useRadarData.ts               # WebSocket 通信封装（核心数据 Hook）
│   ├── useAIAgent.ts                 # AI 代理逻辑 Hook
│   └── useKeyboardControl.ts         # 键盘控制 Hook
│
├── utils/                            # 工具函数
│   ├── difficultyUtils.ts            # 难度相关工具
│   └── repetitionUtils.ts            # 重复次数工具
│
├── managers/                         # 管理器
│   └── AudioManager.ts              # 音频管理器（单例模式）
│
├── icons/                            # SVG 图标组件
│   ├── PrimaryAirIcon.tsx            # 主要空中目标
│   ├── SecondaryAirIcon.tsx          # 次要空中目标
│   ├── PrimaryNavalIcon.tsx          # 主要海上目标
│   ├── SecondaryNavalIcon.tsx        # 次要海上目标
│   ├── PrimaryAntiAircraftArtilleryIcon.tsx   # 主要防空火炮
│   ├── SecondaryAntiAircraftArtilleryIcon.tsx  # 次要防空火炮
│   ├── MissileUpIcon.tsx             # 向上导弹图标
│   └── MissileDownIcon.tsx           # 向下导弹图标
│
└── data/                             # 模拟数据
    └── mockUnknownTargets.ts         # 模拟未知目标数据
```

### 4.2 组件层级关系

```
App.tsx（主应用）
├── InitialFormModal          （初始表单弹窗）
├── CompletionModal           （任务完成弹窗）
├── DifficultyChangeModal     （难度变化弹窗）
├── TaskInfoDisplay           （任务信息显示）
│
├── Radar                     （雷达视图 —— 主视图之一）
│   ├── RadarButtons          （控制按钮面板）
│   ├── RadarDisplay          （Canvas 雷达渲染）
│   │   ├── RadarRenderers    （网格/刻度渲染）
│   │   ├── ScanLine          （扫描线动画）
│   │   ├── UnknownTargetManager
│   │   │   └── UnknownTarget （单个未知目标）
│   │   ├── EnemyTarget       （敌方目标）
│   │   ├── TDC               （目标指定光标）
│   │   ├── VelocityVector    （速度矢量）
│   │   ├── HorizonHUD        （地平线抬头显示）
│   │   ├── NavigationHUD     （导航抬头显示）
│   │   ├── AntennaElevationMarker（天线仰角标记）
│   │   └── ConnectionStatus  （连接状态）
│   └── AntennaElevationControl（天线仰角控制）
│
├── SAPage                    （SA态势感知视图 —— 主视图之二）
│   ├── ThreatAssistant       （威胁助手）
│   └── SAButtons             （SA按钮）
│
├── AIAssistant               （AI 助手面板）
│   └── AIChat                （AI 聊天对话）
│
├── CommunicationLog          （通信日志）
└── ThreatList                （威胁列表）
```

### 4.3 状态管理

#### RadarStore（雷达核心状态）

| 状态类别 | 关键字段 | 说明 |
|---------|---------|------|
| 系统状态 | `isStarted`, `userId`, `taskId` | 系统启动、用户、任务 |
| 雷达参数 | `radarRange`, `scanAngle` | 范围(10/20/40/80nm)、扫描角度 |
| 天线状态 | `currentAntennaElevation`, `targetAntennaElevation` | 天线仰角(-30°~30°) |
| 目标锁定 | `lockedTargetId`, `lockScreenX` | 当前锁定目标 |
| 任务进度 | `completedTaskTypes`, `completionModalInfo` | 任务完成追踪 |

#### AgentStore（AI 代理状态）

| 状态类别 | 关键字段 | 说明 |
|---------|---------|------|
| AI 激活 | `isAIActive`, `currentAILevel` | AI 开关与等级 |
| 操作归属 | `currentOperationOwner` | 'AI' 或 'manual' |
| 推荐 | `serverAIRecommendation` | 服务器推荐 |
| 音频 | `audioEnabled` | 音频开关 |

### 4.4 WebSocket 通信（useRadarData Hook）

- **连接管理**：`GlobalWebSocketManager` 单例模式
- **连接地址**：`ws://localhost:8080/ws`
- **自动重连**：支持断线自动重连

**发送的消息类型：**

| 消息类型 | 说明 |
|---------|------|
| `task_start` | 初始化任务 |
| `settings_update` | 雷达参数更新 |
| `target_selected` | 目标选择 |
| `antenna_adjusted` | 天线调整完成 |
| `SwitchSA` | 切换到 SA 页面 |
| `ResetSA` | 重置 SA 任务 |
| `reset_targets` | 重置目标 |

**接收的消息类型：**

| 消息类型 | 说明 |
|---------|------|
| `init_settings` | 初始设置（任务ID、配置） |
| `radar_data` | 雷达数据更新 |
| `externalTargets` | 外部目标数据 |
| `SAThreats` / `sa_task_updated` | SA 威胁数据 |
| `SAEmergency` | SA 紧急事件 |
| `adjust_antenna` | 天线调整指令 |
| `all_tasks_completed` | 所有任务完成 |

### 4.5 关键功能模块

1. **雷达系统**：扫描模式控制、目标跟踪/锁定、IFF 敌我识别、参数调整
2. **SA 态势感知**：威胁图标显示（空中/海上/防空/导弹）、优先级排序、临机事件处理
3. **AI 辅助系统**：多等级 AI 配置、自动操作（带延迟模拟）、操作归属区分
4. **音频系统**：`AudioManager` 单例，支持导弹发射、威胁升级、雷达操作等音效
5. **任务系统**：支持雷达目标锁定和 SA 威胁响应两种任务，支持重复练习和难度调整

---

## 五、后端代码结构（server/）

### 5.1 目录结构

```
server/
├── main.py                          # 主入口文件（异步启动）
├── start.py                         # 启动脚本（带环境检查）
├── requirements.txt                 # Python 依赖列表
│
├── core/                            # 核心业务逻辑
│   ├── __init__.py
│   └── message_handler.py           # 消息处理器（处理所有客户端消息路由）
│
├── managers/                        # 功能管理器
│   ├── __init__.py
│   ├── config_manager.py            # 配置管理器（加载 agent_level.json）
│   ├── database_manager.py          # 数据库管理器（异步写入队列）
│   ├── logger_manager.py            # 日志管理器（彩色终端输出）
│   ├── task_manager.py              # 任务场景管理器（进度、队列）
│   ├── target_manager.py            # 雷达目标管理器（生成、移动、识别）
│   └── threat_manager.py            # SA 威胁管理器（威胁生成、紧急事件）
│
├── network/                         # 网络通信层
│   ├── __init__.py
│   ├── http_server.py               # HTTP 服务器（静态文件 + WebSocket）
│   ├── websocket_server.py          # 独立 WebSocket 服务器（可选）
│   └── message_protocol.py          # 消息协议定义（增强版 + 传统版）
│
├── models/                          # 数据模型
│   ├── __init__.py
│   └── threat_models.py             # 威胁数据模型（RadarConfig, EnhancedThreat 等）
│
├── services/                        # 业务服务
│   ├── __init__.py
│   ├── priority_calculator.py       # 威胁优先级计算（权重/距离/难度调整）
│   └── position_calculator.py       # 威胁位置计算（均匀分布/碰撞检测）
│
├── data/                            # 数据存储
│   ├── radar_operations.db          # SQLite 主数据库
│   └── data_backup.py               # 数据库备份工具
│
├── tests/                           # 测试文件
│   ├── test.py                      # 主测试
│   ├── test_imports.py              # 导入测试
│   ├── test_connection.py           # 连接测试
│   ├── test_threat_models.py        # 威胁模型测试
│   ├── test_websocket_protocol.py   # WebSocket 协议测试
│   ├── test_enhanced_threat_generation.py  # 增强威胁生成测试
│   ├── test_performance_comparison.py      # 性能对比测试
│   └── test_end_to_end_integration.py      # 端到端集成测试
│
├── docs/                            # 项目文档
│   ├── project_structure.md         # 项目结构说明
│   ├── logging_guide.md             # 日志使用指南
│   ├── task_completion_status.md    # 任务完成状态说明
│   └── README_refactoring.md        # 重构文档
│
└── legacy/                          # 旧版备份
    ├── radar_server.py              # 原始单文件服务器
    └── radar_server_backup.py       # 备份文件
```

### 5.2 各模块详细说明

#### 5.2.1 入口文件

| 文件 | 说明 |
|------|------|
| `main.py` | 异步入口，初始化数据库、管理器，启动 HTTP + WebSocket 服务器 |
| `start.py` | 启动脚本，带环境检查（依赖、配置、数据库）和友好提示 |

**启动参数：**
- `--static-dir`：静态文件目录（默认 `../dist`）
- `--http-port`：HTTP 端口（默认 `8080`）
- `--ws-only`：仅启动 WebSocket 服务器

#### 5.2.2 核心业务逻辑（core/）

**`message_handler.py`** — 消息处理器，系统的核心调度中心：

| 处理的消息类型 | 说明 |
|--------------|------|
| `task_start` | 任务开始（创建任务管理器、初始化目标） |
| `settings_update` | 雷达参数更新（验证 range=80, scanAngle=30） |
| `antenna_adjusted` | 天线调整完成（验证天线高度） |
| `target_selected` | 目标选择（记录操作、标记任务） |
| `threat_clicked` | 威胁点击（记录操作、标记完成） |
| `record_operation` | 记录单个操作 |
| `record_bulk_operations` | 批量记录操作 |
| `SwitchSA` / `ResetSA` | SA 任务切换/重置 |

#### 5.2.3 功能管理器（managers/）

| 管理器 | 说明 |
|-------|------|
| `config_manager.py` | 加载 `agent_level.json` 配置，提供 AI 等级、难度、执行顺序等 |
| `database_manager.py` | SQLite 异步写入（后台线程队列）、表自动创建/更新、WAL 模式 |
| `logger_manager.py` | 彩色日志输出、模块化日志器、文件日志可选 |
| `task_manager.py` | 用户进度持久化、AI/手动任务队列、重复次数控制、练习模式 |
| `target_manager.py` | 目标生成（基于难度）、威胁评估算法、目标动态移动、敌友识别 |
| `threat_manager.py` | 威胁生成（增强版+传统版）、紧急事件生成、自动推送 |

#### 5.2.4 网络通信（network/）

| 文件 | 说明 |
|------|------|
| `http_server.py` | aiohttp HTTP 服务器：静态文件服务（SPA 支持） + WebSocket 路由 `/ws` |
| `websocket_server.py` | 独立 WebSocket 服务器（端口 8765，可选模式） |
| `message_protocol.py` | 消息协议定义、增强协议支持、`LegacyMessageAdapter` 向后兼容 |

#### 5.2.5 数据模型（models/）

| 模型 | 说明 |
|------|------|
| `RadarConfig` | 雷达配置（中心点、半径、画布尺寸） |
| `ThreatPosition` | 威胁位置（x, y 坐标） |
| `EnhancedThreat` | 增强威胁（ID、类型、标签、位置、优先级、分数等） |
| `ThreatGenerationResult` | 威胁生成结果 |

#### 5.2.6 业务服务（services/）

| 服务 | 说明 |
|------|------|
| `priority_calculator.py` | 威胁分数计算（类型权重/距离因子）、分数归一化、难度调整 |
| `position_calculator.py` | 威胁位置计算（均匀角度分布+随机偏移）、碰撞检测、边界检查 |

### 5.3 数据库设计

使用 **SQLite**（WAL 模式），包含以下三张主要表：

#### user_operations（用户操作记录）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 ID |
| task_id | INTEGER | 任务 ID |
| operation_type | TEXT | 操作类型 |
| timestamp | INTEGER | 操作时间戳（毫秒） |
| receive_timestamp | INTEGER | 服务端接收时间戳 |
| is_active | INTEGER | 是否激活 |
| parameters | TEXT | 参数（JSON） |
| user_id | TEXT | 用户 ID |
| event_owner | TEXT | 事件所有者（AI/manual） |
| is_correct | TEXT | 是否正确 |

#### task_settings（任务设置）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 ID |
| task_id | INTEGER UNIQUE | 任务 ID |
| user_id | TEXT | 用户 ID |
| event_owner | TEXT | 事件所有者 |
| repetition_count | INTEGER | 重复次数 |
| is_ai_active | BOOLEAN | AI 是否激活 |
| ai_level_config | TEXT | AI 级别配置（JSON） |
| difficulty_config | TEXT | 难度配置（JSON） |
| ai_level_name | TEXT | AI 级别名称 |
| difficulty_name | TEXT | 难度名称 |

#### user_progress（用户进度）

| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | TEXT (PK) | 用户 ID |
| task_type | TEXT (PK) | 任务类型 |
| ai_queue_json | TEXT | AI 队列（JSON） |
| manual_queue_json | TEXT | 手动队列（JSON） |
| is_ai_completed | BOOLEAN | AI 模式完成 |
| is_manual_completed | BOOLEAN | 手动模式完成 |
| ai_current_scenario_json | TEXT | AI 当前场景 |
| ai_repetition_counter | INTEGER | AI 重复计数 |
| manual_current_scenario_json | TEXT | 手动当前场景 |
| manual_repetition_counter | INTEGER | 手动重复计数 |

### 5.4 WebSocket 通信协议

#### 消息格式

```json
{
  "type": "消息类型",
  "timestamp": 1234567890,
  "...": "其他字段"
}
```

#### 增强威胁消息（sa_task_updated）

```json
{
  "type": "sa_task_updated",
  "threats": [
    {
      "id": "threat_1",
      "type": "PrimaryAir",
      "label": "F-15",
      "position": { "x": 100, "y": 200 },
      "priority": "high",
      "score": 0.95,
      "distance_from_center": 150.5,
      "is_missile": false
    }
  ],
  "radar_config": {
    "center_x": 400, "center_y": 360,
    "radius1": 100, "radius2": 180, "radius3": 306,
    "canvas_width": 800, "canvas_height": 600
  },
  "highest_priority_threat_id": "threat_1"
}
```

#### 增强紧急事件（SAEmergency）

```json
{
  "type": "SAEmergency",
  "event": "upgrade|missile",
  "enhanced_data": {
    "upgrade_count": 1,
    "affected_threat_ids": ["threat_1"],
    "missile_type": "MissileUp|MissileDown"
  },
  "missile_threat": { "..." },
  "updated_threats": [ "..." ]
}
```

---

## 六、核心业务流程

### 6.1 雷达目标识别任务

```
1. 客户端发送 task_start（用户ID、AI配置）
2. 服务器创建任务，获取下一个场景（从任务队列）
3. 初始化雷达目标（基于难度配置生成目标数量和位置）
4. 返回 init_settings（任务ID、设置、重复信息）
5. 客户端发送 settings_update（设置雷达参数）
6. 服务器验证参数（range=80, scanAngle=30）
7. 返回 adjust_antenna（天线调整指令）
8. 客户端发送 antenna_adjusted（天线高度）
9. 服务器验证高度，开始推送目标数据
10. 客户端选择目标（target_selected）
11. 服务器记录操作，判定正确性，标记任务完成
```

### 6.2 SA 威胁应对任务

```
1. 客户端发送 SwitchSA / ResetSA
2. 服务器生成增强威胁数据（位置、优先级、分数）
3. 返回 sa_task_updated（完整威胁信息）
4. 2-3秒后自动触发紧急事件：
   - 升级事件：威胁类型升级 / 位置重新分布
   - 导弹事件：添加新导弹威胁
5. 客户端点击最高优先级威胁（threat_clicked）
6. 服务器记录操作，标记任务完成
```

### 6.3 数据流向

```
[用户操作] → 前端组件 → RadarStore/AgentStore → useRadarData → WebSocket发送
                                                                    ↓
                                                              后端 message_handler
                                                                    ↓
                                                         managers（目标/威胁/任务）
                                                                    ↓
                                                              WebSocket推送
                                                                    ↓
[界面更新] ← 前端组件 ← RadarStore/AgentStore ← useRadarData ← WebSocket接收
```

---

## 七、配置管理

### 配置文件：`public/agent_level.json`

```json
{
  "current_level": "L2",
  "levels": [
    {
      "level": "L0|L1|L2",
      "desc": "等级描述",
      "threat_select_delay_ms": 1000,
      "tdc_select_delay_ms": 1000,
      "decision_probabilities": [0.3, 0.5]
    }
  ],
  "game_settings": {
    "current_difficulty": "high|low",
    "audio_enabled": true,
    "max_repetitions": 10,
    "practice_repetitions": 1,
    "difficulty_levels": {
      "high": { "name": "高", "threat_count": 10, "target_count": 10 },
      "low": { "name": "低", "threat_count": 5, "target_count": 5 }
    },
    "execution_order": {
      "difficulty_order": ["high", "low"],
      "level_order": ["L0", "L1", "L2"],
      "audio_options": [true, false]
    }
  }
}
```

---

## 八、部署方式

### 开发环境

```bash
# 启动前端开发服务器（默认 http://localhost:5173）
pnpm dev

# 启动后端服务器（默认 http://localhost:8080）
cd server && python main.py
```

### 生产环境

```bash
# 1. 构建前端
pnpm build          # 输出到 dist/

# 2. 启动后端（同时提供静态文件服务）
cd server && python main.py --static-dir ../dist --http-port 8080
```

**部署架构**：后端 HTTP 服务器同时提供静态文件服务和 WebSocket 端点，适合单机部署。

---

## 九、主要依赖

### 前端依赖

| 包名 | 版本 | 用途 |
|------|------|------|
| react | ^18.3.1 | UI 框架 |
| react-dom | ^18.3.1 | DOM 渲染 |
| react-konva | ^18.2.10 | Canvas 图形渲染 |
| konva | ^9.3.18 | 2D 图形库 |
| mobx | ^6.13.7 | 状态管理 |
| mobx-react-lite | ^4.1.0 | MobX React 绑定 |
| react-hot-toast | ^2.5.2 | 通知提示 |
| react-modal | ^3.16.3 | 模态框 |
| typescript | ^5.7.3 | 类型系统 |
| vite | ^5.4.14 | 构建工具 |
| tailwindcss | ^3.4.17 | CSS 框架 |

### 后端依赖

| 包名 | 版本 | 用途 |
|------|------|------|
| aiohttp | 3.12.14 | 异步 HTTP/WebSocket 服务器 |
| websockets | 15.0.1 | WebSocket 支持 |
| numpy | 1.26.4 | 数值计算（目标位置） |
| matplotlib | 3.10.1 | 数据可视化 |
| colorama | 0.4.6 | 终端彩色输出 |

---

## 十、总结

F18-Radar 是一个架构清晰、功能完善的雷达模拟训练系统：

- **前端**：基于 React + TypeScript + MobX 构建，使用 Konva Canvas 渲染雷达画面，通过 WebSocket 实时通信
- **后端**：基于 Python + aiohttp 的异步架构，模块化设计，支持目标生成、威胁管理、任务调度等核心功能
- **通信**：全双工 WebSocket 实时通信，支持增强协议和传统协议向后兼容
- **数据**：SQLite 持久化存储操作记录和用户进度，支持异步写入避免阻塞
- **特色功能**：AI 多等级辅助、难度动态调整、操作录制分析、练习/正式双模式
