# 外部设备WebSocket客户端集成说明

## 概述

雷达系统现在作为**WebSocket客户端**连接到8765端口的外部设备服务，监听ForceSwitch控制数据，并将其转换为TDC坐标广播给雷达前端。

## 架构设计

### WebSocket客户端架构

```
外部设备服务 (8765端口)  ←  雷达WebSocket客户端  →  雷达前端 (8080端口)
     ↓                        ↓                        ↓
ForceSwitch数据         监听并转换数据              TDC位置更新
```

### 数据流转换

```json
// 从外部设备服务接收 (8765端口)
{
  "RawInput": 0.0,
  "YawInput": 0.04,
  "PitchInput": 0.0,
  "Fire": false,
  "Throttle": 0.0,
  "PawnControl": false,
  "ForcePress": false,
  "ForceSwitch": [0.5, -0.3],  // ← TDC坐标数据
  "timestamp": 1759229118.9065819
}

// 转换后广播给雷达客户端 (8080端口)
{
  "type": "tdc_coordinate",
  "x": 0.5,
  "y": -0.3,
  "timestamp": 1759229118.9065819,
  "source": "external_device"
}
```

## 主要功能

### 1. 自动连接和重连
- 启动时自动尝试连接到外部设备服务
- 连接断开时自动重连（5秒间隔）
- 连接失败时显示详细错误信息

### 2. 实时状态监控
- 连接状态实时显示
- 消息接收计数
- 雷达客户端连接数统计

### 3. 数据转换和广播
- 提取ForceSwitch数据
- 转换为标准TDC坐标格式
- 广播给所有连接的雷达客户端

## 使用方法

### 启动雷达系统

```bash
# 标准启动（自动连接外部设备服务）
cd server
python main.py

# 自定义外部设备服务端口
python main.py --external-port 9999

# 禁用外部设备客户端
python main.py --no-external
```

### 测试外部设备服务

1. **启动外部设备服务模拟器**：
   ```bash
   python test_8765_server_simulator.py
   ```

2. **启动雷达系统**：
   ```bash
   cd server
   python main.py
   ```

## 状态显示

### 启动时显示
```
🚀 正在启动外部设备WebSocket客户端...
🎯 目标服务: ws://localhost:8765
📋 客户端状态:
   - 雷达客户端连接数: 0
   - 自动重连间隔: 5秒
--------------------------------------------------
```

### 连接成功显示
```
🔌 尝试连接到外部设备服务: ws://localhost:8765
✅ 【连接成功】已连接到外部设备服务!
   📍 服务地址: ws://localhost:8765
   📊 雷达客户端连接数: 1
   🎯 开始监听ForceSwitch数据...
```

### 消息接收显示
```
📨 【消息 #1】外部设备数据包
   📄 原始数据: {"ForceSwitch": [0.5, -0.3], ...}
   ⏰ 接收时间: 18:45:32
解析后数据: {
  "ForceSwitch": [0.5, -0.3],
  "timestamp": 1759229118.9065819
}
==============================
📍 ForceSwitch提取: [0.500000, -0.300000]
⏰ 时间戳: 1759229118.9065819
🎯 转换为TDC消息: {...}
📡 准备广播给 1 个雷达客户端
✅ 成功广播给 1 个雷达客户端
```

### 连接失败显示
```
❌ 无法连接到外部设备服务 ws://localhost:8765
   可能原因: 外部设备服务未启动或端口不正确
⏳ 5秒后尝试重新连接...
```

## 错误处理

### 1. 连接错误
- **ConnectionRefusedError**: 外部设备服务未启动
- **TimeoutError**: 连接超时
- **其他网络错误**: 显示详细错误信息

### 2. 自动重连
- 连接断开后自动尝试重连
- 可配置重连间隔（默认5秒）
- 无限重试直到连接成功

### 3. 数据验证
- ForceSwitch数据范围验证
- JSON解析错误处理
- 广播失败处理

## 配置选项

### 命令行参数
- `--external-port`: 外部设备服务端口（默认8765）
- `--no-external`: 禁用外部设备客户端
- 其他雷达系统参数保持不变

### 代码配置
```python
# 在 external_device_server.py 中
external_device_client = ExternalDeviceClient(
    host="localhost",           # 外部设备服务地址
    port=8765,                 # 外部设备服务端口
)
external_device_client.reconnect_interval = 5  # 重连间隔（秒）
```

## 测试工具

### 1. 外部设备服务模拟器
- **文件**: `test_8765_server_simulator.py`
- **功能**: 模拟外部设备服务，发送测试数据
- **特性**: 自动发送圆形运动轨迹的ForceSwitch数据

### 2. 连接测试工具
- **文件**: `test_8765_connection.py`（需要更新为客户端测试）
- **功能**: 测试与外部设备服务的连接

## 与前端的集成

雷达前端无需修改，因为：
1. 外部设备客户端将ForceSwitch数据转换为标准的`tdc_coordinate`消息
2. 前端已经支持接收和处理`tdc_coordinate`消息
3. 混合控制逻辑自动处理外部设备、内部WebSocket和键盘控制的优先级

## 优势

1. **解耦设计**: 雷达系统作为客户端，不依赖外部设备的连接时机
2. **自动重连**: 外部设备服务重启时自动恢复连接
3. **状态透明**: 详细的连接和数据处理状态显示
4. **向后兼容**: 不影响现有的键盘和内部WebSocket控制功能
5. **易于测试**: 提供完整的测试工具和模拟器

## 故障排除

### 常见问题

1. **无法连接到外部设备服务**
   - 检查外部设备服务是否在8765端口运行
   - 确认防火墙设置
   - 验证网络连接

2. **连接频繁断开**
   - 检查外部设备服务的稳定性
   - 调整重连间隔
   - 查看网络质量

3. **数据不更新**
   - 确认外部设备服务正在发送数据
   - 检查ForceSwitch字段格式
   - 验证雷达客户端连接状态

这个客户端架构为雷达系统提供了灵活、可靠的外部设备集成能力。
