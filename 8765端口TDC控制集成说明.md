# 8765端口TDC控制集成说明

## 🎯 功能概述

现在TDC（Target Designation Cursor）已经成功集成了8765端口的外部设备控制！系统支持以下功能：

- ✅ **外部设备控制**: 通过WebSocket连接8765端口接收ForceSwitch数据
- ✅ **混合控制模式**: 外部控制优先，键盘控制作为备用
- ✅ **实时坐标转换**: 归一化坐标(-1到1)自动转换为像素坐标
- ✅ **变化检测**: 只有数据变化超过阈值才处理，避免频繁更新
- ✅ **状态显示**: 实时显示连接状态和控制信息

## 🔧 技术实现

### 1. 外部设备数据格式
外部设备需要发送以下JSON格式的数据到8765端口：

```json
{
  "RawInput": 0.0,
  "YawInput": 0.0,
  "PitchInput": 0.0,
  "Fire": false,
  "Throttle": 0.0,
  "PawnControl": false,
  "ForcePress": false,
  "ForceSwitch": [0.5, -0.3],  // TDC坐标数据 [x, y]，范围 -1 到 1
  "timestamp": 1640995200000
}
```

### 2. 坐标系统
- **输入**: ForceSwitch数组 `[x, y]`，范围 -1 到 1
- **输出**: 像素坐标，自动映射到雷达显示区域
- **坐标映射**:
  - (-1, -1) → 雷达显示区域左上角
  - (0, 0) → 雷达显示区域中心
  - (1, 1) → 雷达显示区域右下角

### 3. 控制优先级
1. **外部设备控制** (最高优先级)
   - 当8765端口有数据更新时，立即响应
   - 禁用键盘控制2秒
   
2. **键盘控制** (备用)
   - 仅在外部控制不活跃时启用
   - 使用方向键控制TDC移动

## 📊 状态监控

### 连接状态显示
当外部设备连接成功时，雷达界面左上角会显示状态面板：

```
🎮 外部TDC控制
状态: 已连接
消息: 125
坐标: (0.500, -0.300)
键盘控制: 禁用
```

### 状态说明
- **状态**: 显示WebSocket连接状态
- **消息**: 接收到的消息总数
- **坐标**: 当前TDC的归一化坐标
- **键盘控制**: 当前键盘控制的启用状态

## 🚀 使用方法

### 1. 启动系统
```bash
# 启动雷达服务器（注意：服务器端转发已禁用）
cd server
python main.py

# 启动前端
npm run dev
```

### 2. 连接外部设备
- 外部设备连接到 `ws://localhost:8765`
- 发送包含ForceSwitch数据的JSON消息
- 系统会自动处理坐标转换和TDC更新

### 3. 测试功能
可以使用提供的测试脚本：

```bash
# 启动8765端口模拟器
python test_8765_server_simulator.py

# 发送测试数据
python test_external_device_simulator.py
```

## 🔍 调试功能

### 控制台日志
系统会输出详细的调试信息：

```
[useExternalTDCControl] ForceSwitch变化: [0.500000, -0.300000]
[useExternalTDCControl] TDC坐标已更新: 归一化(0.500000, -0.300000) -> 像素(640.0, 360.0)
[Radar] 外部TDC坐标更新: { normalized: {x: 0.5, y: -0.3}, pixel: {x: 640, y: 360}, source: 'external_device' }
[TDC键盘] WebSocket控制活跃，忽略up操作
```

### 变化检测
- 默认阈值: 0.001
- 只有坐标变化超过阈值才会处理
- 可通过Hook接口动态调整阈值

## ⚙️ 配置选项

### 1. WebSocket地址
```typescript
// 在Radar.tsx中修改
const externalTDC = useExternalTDCControl(
  'ws://localhost:8765',  // 修改为实际的WebSocket地址
  coordinateConfig,
  handleExternalTDCUpdate
);
```

### 2. 变化检测阈值
```typescript
// 动态调整阈值
externalTDC.setChangeThreshold(0.01); // 设置为0.01
```

### 3. 控制超时时间
```typescript
// 修改键盘控制的启用逻辑
const enableKeyboardControl = !hasRecentWebSocketControl && !externalTDC.isTDCControlActive(3000); // 3秒超时
```

## 🛠️ 故障排除

### 1. 连接问题
- 确保8765端口没有被其他程序占用
- 检查防火墙设置
- 验证WebSocket地址是否正确

### 2. 坐标不准确
- 检查ForceSwitch数据范围是否在-1到1之间
- 验证雷达显示区域配置是否正确
- 查看控制台的坐标转换日志

### 3. 控制冲突
- 外部控制活跃时键盘控制会自动禁用
- 检查状态面板中的"键盘控制"状态
- 等待2秒后键盘控制会自动恢复

## 📝 开发说明

### 核心文件
- `src/hooks/useExternalTDCControl.ts` - 外部TDC控制Hook
- `src/components/Radar.tsx` - 主雷达组件，集成外部控制
- `src/components/TDC.tsx` - TDC显示组件
- `src/components/RadarRenderers.tsx` - TDC渲染逻辑

### 扩展功能
如需添加更多外部控制功能，可以：
1. 扩展`ExternalDeviceMessage`接口
2. 在`processExternalMessage`中添加处理逻辑
3. 通过回调函数传递给父组件

## 🎉 总结

8765端口的TDC控制已经完全集成到雷达系统中，提供了：
- 🔄 **无缝切换**: 外部控制和键盘控制的智能切换
- 📡 **实时响应**: 低延迟的坐标更新和显示
- 🎯 **精确控制**: 高精度的坐标转换和映射
- 📊 **状态监控**: 完整的连接状态和调试信息
- 🛡️ **稳定可靠**: 自动重连和错误处理机制

现在您可以通过8765端口的外部设备直接控制雷达系统中的TDC光标了！
