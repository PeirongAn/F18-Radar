# 功能需求列表 - 按实现难度分类

## 🟢 容易实现 (Easy)

### A. 传感器任务：
**A2.** 接管按钮近一些，加入提示和快捷键（F10）
- 理由：已有键盘控制功能，只需调整UI位置和添加快捷键绑定

**A3.** 锁定完成后自动打开"IFF", 表示任务完成
- 理由：已有目标锁定逻辑，只需在锁定成功后自动切换IFF状态

**A4.** 重置按钮，刷新仪表盘，重新做任务，无需再输入用户信息
- 理由：已有handleReset函数，只需优化重置逻辑避免重新输入用户信息

### B. 威胁排序：
**B2.** 点击最具威胁项后，加入弹窗提示"任务结束"
- 理由：已有威胁点击处理逻辑，只需添加弹窗组件

**B3.** 仪表盘随机转动
- 理由：已有rotation随机角度逻辑，可直接应用

**B4.** 接管按钮同"传感器任务"
- 理由：与A2相同的实现

**B6.** "交互工效"，加入音频开关控制是否播放
- 理由：已有AudioManager音频管理器，只需添加开关控制

## 🟡 中等难度 (Medium)

### A. 传感器任务：
**A1.** 智能体选择敌机目标，根据不同等级加入一定错误概率（当前只有延时）
- 理由：已有AI智能体基础框架和等级配置，需要扩展错误概率机制

**A6.** "交互工效"，地面回复失真，可以体现在未收到天线参数调整指令，导致目标未出现
- 理由：需要新增失真逻辑，模拟通信不可靠性，修改现有天线调整流程

### B. 威胁排序：
**B1.** 临机事件不一定需要一定有高优事件，可以只有中等优先（但可以选出最具威胁项）
- 理由：已有临机事件生成逻辑，需要调整优先级分配算法和威胁评估逻辑

## 🔴 困难实现 (Hard)

### A. 传感器任务：
**A5.** "动态容限"，目标个数随着任务难度而增多（留出接口）
- 理由：需要设计完整的难度系统，包括难度评估、目标数量动态调整、性能评价等复杂逻辑

### B. 威胁排序：
**B5.** "动态容限"，同"传感器任务"
- 理由：同A5，需要为威胁排序任务设计相应的动态难度系统

---
**代码分析总结：**
- 现有代码基础较好，已实现AI智能体、音频管理、WebSocket通信、数据库记录等核心功能
- 大部分UI和交互改进属于容易实现范畴
- 动态难度系统是最复杂的功能，需要完整的架构设计