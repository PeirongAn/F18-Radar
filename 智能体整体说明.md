# 辅助智能模块设计方法

## 1. 与前端系统的耦合方式

### 1.1 MobX状态管理集成
辅助智能模块通过 `AgentStore` 无缝集成到现有的MobX状态管理体系中，采用响应式编程模式实现智能行为的统一管理。

```typescript
// 智能体状态管理
class AgentStore {
  isAIActive: boolean = false;           // AI激活状态
  currentAILevel: string = "";           // 当前AI等级
  aiConfigs: AgentLevelConfig[] = [];    // AI配置数组
  currentOperationOwner: 'AI' | 'manual' = 'manual'; // 操作归属
}
```

### 1.2 Hook模式的行为协调
通过自定义Hook `useAIAgent` 实现智能行为与UI组件的松耦合，支持多种任务场景的统一处理模式。

```typescript
// 统一的AI行为协调接口
useAIAgent({
  isActive: boolean,        // AI激活状态
  emergency: any,           // 事件触发器
  onHandleEmergency: Function, // 事件处理回调
  getBestThreat?: Function  // 决策函数（可选）
});
```

### 1.3 事件驱动的交互模式
- **状态响应**：通过 `useEffect` 监听状态变化，自动触发AI行为
- **事件标记**：所有AI操作标记 `event_owner: 'AI'`，与用户操作明确区分
- **延迟模拟**：通过 `setTimeout` 模拟真实的人类反应时间

### 1.4 配置驱动的行为控制
智能体行为完全由外部配置文件 `agent_level.json` 驱动，实现运行时的动态调整能力。

```json
{
  "current_level": "L2",
  "levels": [
    {
      "level": "L0",
      "threat_select_delay_ms": 1000,
      "tdc_select_delay_ms": 1000,
      "decision_probabilities": [0.3, 0.5]
    }
  ]
}
```

## 2. 智能体等级设计

### 2.1 分层智能架构
系统采用三级智能体等级设计，模拟从新手到专家的操作水平：

| 等级 | 描述 | 反应延迟 | 准确率范围 |
|------|------|----------|------------|
| L0 | 低级智能体 | 1000ms | 30%-50% |
| L1 | 中级智能体 | 500ms | 70%-90% |
| L2 | 高级智能体 | 100ms | 100% |

### 2.2 多维度性能建模
每个智能体等级通过多个性能维度进行建模：

- **响应延迟**：`threat_select_delay_ms` / `tdc_select_delay_ms`
- **决策准确率**：`decision_probabilities` 数组
- **行为一致性**：通过配置确保行为的可预测性

### 2.3 渐进式能力提升
智能体等级设计遵循渐进式提升原则：
- **延迟递减**：从1000ms → 500ms → 100ms
- **准确率递增**：体现学习和经验积累过程
- **决策复杂度**：高级智能体支持更复杂的决策逻辑

## 3. 智能策略设计

### 3.1 概率驱动的决策模型
采用概率模型模拟真实的人类决策过程，支持灵活的准确率控制：

```typescript
// 动态准确率生成
let accuracy = 1.0;
if (probabilities.length === 1) {
  accuracy = probabilities[0];  // 固定准确率
} else if (probabilities.length >= 2) {
  // 在范围内随机生成，模拟性能波动
  const min = probabilities[0];
  const max = probabilities[1];
  accuracy = Math.random() * (max - min) + min;
}
```

### 3.2 二元池选择策略
智能体决策基于"正确池"和"错误池"的概念：

```typescript
if (randomChoice <= accuracy) {
  // 从正确池选择（最优决策）
  choiceIndex = 0;
} else {
  // 从错误池选择（模拟失误）
  choiceIndex = Math.floor(Math.random() * (alternatives.length - 1)) + 1;
}
```

### 3.3 上下文感知的适应性
- **任务适配**：同一智能体配置适用于不同任务场景
- **实时调整**：支持运行时切换智能体等级
- **回退机制**：在配置缺失时提供合理的默认行为

### 3.4 人机协作模式
- **无缝切换**：支持AI模式与手动模式的动态切换
- **操作透明**：所有AI操作都有明确的日志记录
- **用户控制**：用户可随时接管控制权

## 4. 设计优势

### 4.1 可扩展性
- **模块化设计**：智能体逻辑与业务逻辑分离
- **配置驱动**：新增智能体等级无需修改代码
- **接口统一**：不同任务使用相同的AI集成模式

### 4.2 可测试性
- **行为可预测**：基于配置的确定性行为
- **性能可测量**：准确率和延迟的量化指标
- **场景可重现**：配置文件确保测试环境一致性

### 4.3 用户体验
- **渐进式学习**：从低级到高级的能力展示
- **真实感模拟**：延迟和错误率模拟真实操作员
- **灵活控制**：用户可根据需要选择合适的AI等级

这种设计方法实现了智能辅助功能与现有系统的深度融合，既保持了系统的灵活性，又提供了丰富的智能化体验。