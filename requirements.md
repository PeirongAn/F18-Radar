# 改变的需求点：

- 1. 需要在radar_server.py 中读取配置项，这个配置项是写在../public/agent_level.json. 该文件中需要增加难度列表，当前难度，是否开启音频。难度列表中，包含2个等级，低等级的威胁数量和目标少（5）个，高等级的威胁数量和目标多（10）个
- 2. 前台src 中，IFF 由自动开启，改为手动开启
- 3. 我发现好像日志记录到数据库好像失效了，帮我review一下

# 修改为系统
- 1. 每个用户登录后有3次练习的机会，然后开始正式任务。
- 2. 每轮正式任务，使用public/agent_level.json 中的参数设置，每次设置做10次任务，面板右上角提示还剩几次。
- 3. 每轮任务结束后，自动跳转下一种设置
- 4. 将每次设置的参数存储到数据库