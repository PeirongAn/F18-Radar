基于你提供的代码文件，我来分别总结这两个AI辅助操作任务的逻辑：

## 一、传感器（雷达）任务的AI辅助操作逻辑

### 1. AI目标选择逻辑（Radar.tsx）
- **触发条件**：AI激活状态、系统已启动、有可用外部目标且当前无锁定目标
- **决策机制**：
  - 使用 `decision_probabilities` 配置数组控制准确率
  - 如果数组只有一个值，直接使用该准确率
  - 如果有两个或更多值，在最小值和最大值间随机生成准确率
  - 根据随机数与准确率比较，决定从"敌机池"或"友机池"选择目标

### 2. AI操作流程
```javascript
// 延迟执行目标选择（使用 tdc_select_delay_ms）
setTimeout(() => {
  // 1. 移动TDC到目标位置
  setTdcPosition(targetDisplayPosition);
  
  // 2. 选择目标并设置锁定线
  onTargetSelect({
    targetId: finalTargetToSelect.id,
    lockX: lockX,
    event_owner: 'AI'
  });
}, config.tdc_select_delay_ms);
```

### 3. AI自动参数调整
- **雷达范围调整**：根据服务器推荐自动设置RADAR_RANGES
- **扫描角度调整**：支持15°、30°、60°三种模式切换
- **天线仰角调整**：AI自动响应 `antennaAdjustmentRequired` 信号

## 二、情势感知（SA）威胁排序任务的AI辅助操作逻辑

### 1. AI威胁选择逻辑（SAPage.tsx + useAIAgent.ts）
- **触发机制**：
  - AI激活时的初始威胁评估（使用 `getBestThreat`）
  - 临机事件发生时的自动响应（监听 `emergency` 变化）

### 2. AI决策算法（getBestThreat函数）
```javascript
// 根据配置生成准确率
let accuracy = 1.0;
if (probabilities.length === 1) {
  accuracy = probabilities[0];
} else if (probabilities.length >= 2) {
  const min = probabilities[0];
  const max = probabilities[1];
  accuracy = Math.random() * (max - min) + min;
}

// 根据准确率决定选择
if (randomChoice <= accuracy) {
  // 从正确池子选择：选择最佳威胁（排序第一的）
  choiceIndex = 0;
} else {
  // 从错误池子选择：选择非最佳威胁
  choiceIndex = Math.floor(Math.random() * (threatsWithScore.length - 1)) + 1;
}
```

### 3. useAIAgent Hook的协调逻辑
- **双重触发机制**：
  - AI状态从false变为true时的初始评估
  - 临机事件变化时的响应处理
- **延迟控制**：使用 `threat_select_delay_ms` 配置延迟执行
- **防重复处理**：通过 `emergencyId` 和 `lastHandledRef` 确保同一事件只处理一次

### 4. 威胁评分与排序
- **增强协议**：直接使用服务端计算的分数和排序
- **传统协议**：前端计算威胁权重和距离分数
- **统一接口**：两种协议都通过 `handleThreatIconClick` 执行选择操作

## 共同特点

### 1. 配置驱动的AI行为
- 所有AI行为都基于 `AgentStore.currentAILevelConfig` 的配置
- 支持动态调整AI等级和相应的行为参数

### 2. 事件归属标记
- 所有AI操作都标记 `event_owner: 'AI'`
- 与用户手动操作 `event_owner: 'manual'` 区分

### 3. 延迟模拟人类操作
- 雷达任务使用 `tdc_select_delay_ms`
- SA任务使用 `threat_select_delay_ms`
- 模拟真实的人类反应时间

这两个任务的AI系统都采用了概率驱动的决策模型，通过配置化的参数控制AI的"智能水平"和"准确率"，既能模拟专家级操作，也能模拟新手的错误决策。