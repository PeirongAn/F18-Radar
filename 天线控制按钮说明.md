# 天线控制按钮说明

## 🎯 功能概述

雷达系统现在支持通过左侧按钮控制天线高度，实现了以下功能：

- ✅ **左侧第2个按钮**: 天线向上调整 (+1度)
- ✅ **左侧第4个按钮**: 天线向下调整 (-1度)
- ✅ **高度范围**: -3度 到 +3度
- ✅ **键盘控制**: T键向上，B键向下（原有功能保持）

## 🔧 技术实现

### 按钮映射
```
左侧按钮布局:
┌─────────────┐
│ 1. 清除重置  │
│ 2. 天线向上  │ ← 新增功能
│ 3. 扫描角度  │
│ 4. 天线向下  │ ← 新增功能
│ 5. BR计数   │
│ 6. (未使用) │
└─────────────┘
```

### 控制逻辑
```typescript
// 天线向上 (left index = 2)
const newElevation = Math.min(3, radarStore.currentAntennaElevation + 1);
radarStore.setCurrentAntennaElevation(newElevation, 'user', sendMessage);

// 天线向下 (left index = 4)  
const newElevation = Math.max(-3, radarStore.currentAntennaElevation - 1);
radarStore.setCurrentAntennaElevation(newElevation, 'user', sendMessage);
```

## 📊 控制方式对比

| 控制方式 | 向上 | 向下 | 范围 | 精度 |
|---------|------|------|------|------|
| 键盘控制 | T键 | B键 | -3° ~ +3° | 1° |
| 按钮控制 | 左侧按钮2 | 左侧按钮4 | -3° ~ +3° | 1° |

## 🎮 使用方法

### 1. 按钮控制
- 点击**左侧第2个按钮**：天线高度 +1度
- 点击**左侧第4个按钮**：天线高度 -1度
- 每次点击调整1度，范围限制在-3度到+3度之间

### 2. 键盘控制（原有功能）
- 按**T键**：天线高度 +1度
- 按**B键**：天线高度 -1度

### 3. 状态反馈
- 控制台会输出调整信息：
  ```
  天线向上按钮被点击
  天线高度调整为: 2度
  ```
- 天线高度变化会实时反映在雷达显示中
- 系统会自动向服务器发送高度更新消息

## 🔍 调试信息

### 控制台日志
```
天线向上按钮被点击
天线高度调整为: 1度

天线向下按钮被点击  
天线高度调整为: 0度
```

### 系统集成
- 使用`radarStore.setCurrentAntennaElevation()`方法
- 自动发送消息到服务器：`sendMessage`
- 标记操作来源为`'user'`，区别于AI控制

## ⚙️ 技术细节

### 边界检查
```typescript
// 向上调整时的边界检查
const newElevation = Math.min(3, radarStore.currentAntennaElevation + 1);

// 向下调整时的边界检查  
const newElevation = Math.max(-3, radarStore.currentAntennaElevation - 1);
```

### MobX状态管理
- 使用`radarStore.currentAntennaElevation`获取当前高度
- 通过`radarStore.setCurrentAntennaElevation()`更新状态
- 自动触发UI更新和服务器通信

### 消息协议
系统会向服务器发送天线高度更新消息，格式类似：
```json
{
  "type": "antenna_elevation_update",
  "elevation": 2,
  "source": "user"
}
```

## 🎉 总结

现在雷达系统提供了两种天线控制方式：

1. **键盘控制** (T/B键) - 适合快速操作
2. **按钮控制** (左侧按钮2/4) - 适合鼠标操作

两种方式功能完全一致，用户可以根据操作习惯选择使用。所有操作都会：
- ✅ 实时更新天线高度显示
- ✅ 自动发送更新到服务器
- ✅ 记录操作日志
- ✅ 遵守-3°到+3°的安全范围限制
