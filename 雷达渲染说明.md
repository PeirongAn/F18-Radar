
# 雷达面板功能实现核心要点

## 1. 面板渲染技术

### 1.1 React Konva技术栈
```typescript
import { Stage, Layer, Group, Line } from 'react-konva';
```
**核心特点**：
- **Canvas渲染**：使用Konva.js作为底层Canvas渲染引擎，提供高性能的2D图形渲染
- **组件化架构**：通过React-Konva将Canvas操作封装为React组件，支持声明式编程
- **分层渲染**：采用Stage→Layer→Group的层次结构，便于管理复杂的雷达显示元素

### 1.2 模块化组件设计
```typescript
// 核心渲染组件分离
import { renderMainFrame, renderText } from './RadarRenderers';
import ScanLine from './ScanLine';
import { UnknownTargetManager } from './UnknownTargetManager';
```
**设计优势**：
- **关注点分离**：主框架、扫描线、目标管理各自独立
- **可复用性**：渲染函数可在不同场景下重用
- **维护性**：模块化结构便于功能扩展和bug修复

## 2. 雷达坐标系统与界面布局

### 2.1 坐标系统详解
```typescript
// 极坐标到屏幕坐标的转换
const originX = (framePositions.startX + framePositions.endX) / 2;
const originY = framePositions.endY; // 雷达底边中点作为原点

const pixelsPerNm = radarConfig.mainBoxHeight / range; // 距离比例尺
const angleRadians = angleDegrees * (Math.PI / 180);
const distancePixels = distanceNm * pixelsPerNm;

// 导航坐标系适配（0度朝北）
const offsetX = distancePixels * Math.sin(angleRadians);
const offsetY = -distancePixels * Math.cos(angleRadians);
```

### 2.2 雷达面板轴线含义
**横轴（X轴）**：
- **物理含义**：表示方位角度，东西方向的投影
- **坐标范围**：-180°到+180°（左侧为西，右侧为东）
- **刻度标记**：顶部和底部的刻度线表示不同方位角

**纵轴（Y轴）**：
- **物理含义**：表示距离，从本机（雷达底边中点）向前的距离
- **坐标范围**：0到最大探测距离（如20海里）
- **刻度标记**：左右两侧的刻度线表示不同距离环

**坐标系特点**：
- **原点位置**：雷达显示区域底边中点（代表本机位置）
- **导航坐标系**：0°指向北方，90°指向东方，符合航空导航习惯
- **动态比例尺**：根据雷达量程自动调整像素/海里比例
- **实时位置更新**：支持目标位置的动态跟踪和显示

### 2.3 界面元素布局
```typescript
// 界面关键区域定义
const centerX = startX + radarConfig.mainBoxWidth / 2;  // 水平中心
const centerY = startY + radarConfig.mainBoxHeight / 2; // 垂直中心

// 刻度线布局
顶部刻度：[3, 6, 9, 12, 15] // 对应不同方位角
底部刻度：[3, 6, 9, 12, 15] // 镜像方位角标记
左侧刻度：[1, 2, 3] // 对应距离环
右侧刻度：[1, 2, 3] // 镜像距离环标记
```

## 3. 目标符号渲染方案

### 3.1 服务端数据到目标渲染流程
```typescript
// 1. 接收服务端目标数据
interface RadarTarget {
  id: string;              // 目标唯一标识
  type: 'army' | 'friend'; // 目标类型：敌机/友机
  position: { x: number; y: number }; // 极坐标：x=角度，y=距离
  speed?: number;          // 目标速度
  direction_degrees: number; // 运动方向
  threat_score: number;    // 威胁评分
}

// 2. 坐标转换处理
const processedExternalTargets = React.useMemo(() => {
  return radarData.externalTargets.map(target => {
    const angleDegrees = target.position.x; // 角度
    const distanceNm = target.position.y;   // 距离（海里）
    
    // 转换为屏幕坐标
    const screenX = originX + distancePixels * Math.sin(angleRadians);
    const screenY = originY - distancePixels * Math.cos(angleRadians);
    
    return { ...target, position: { x: screenX, y: screenY } };
  });
}, [radarData?.externalTargets]);
```

### 3.2 统一目标管理器
```typescript
<UnknownTargetManager
  showTargets={!isSilent}
  externalTargets={processedExternalTargets}
  selectedTargetId={radarStore.lockedTargetId}
  verticalLineX={radarStore.lockScreenX}
  iffMode={iffMode}
  onTargetClick={handleTargetClickInIFF}
/>
```

### 3.3 目标符号渲染细节
**符号形状与颜色**：
```typescript
// IFF模式下的颜色区分
const getTargetColor = (target: UnknownTargetData): string => {
  if (!iffMode) return '#00FF00'; // 默认绿色
  
  if (target.type === 'friend') {
    return '#00FF00'; // 绿色 - 友军
  } else if (target.type === 'army') {
    return '#FF0000'; // 红色 - 敌军
  }
  return '#00FF00';
};
```

**符号渲染特色**：
- **类型区分**：敌机（红色）/友机（绿色）采用不同颜色标识
- **状态可视化**：锁定状态用垂直线和边框高亮显示
- **信息叠加**：目标ID、距离、航向等信息实时更新
- **交互响应**：点击目标可获取详细信息和操作选项

### 3.4 目标标号渲染逻辑
```typescript
// 目标信息显示
const targetInfo = calculateTargetInfo(target);
onAddMessage('info', `目标ID: ${target.id}`);
onAddMessage('info', `目标类型: ${target.type === 'army' ? '敌机' : '友机'}`);
onAddMessage('info', `距离: ${targetInfo.distance.toFixed(2)} 海里`);
onAddMessage('info', `运动方向: ${targetInfo.direction.toFixed(1)}°`);
onAddMessage('info', `威胁评分: ${targetInfo.threatScore.toFixed(3)}`);
```

**目标标号内容**：
- **目标ID**：唯一标识符（如T01、T02等）
- **目标类型**：敌机/友机分类
- **距离信息**：以海里为单位的精确距离
- **运动参数**：速度和航向信息
- **威胁评估**：基于角度和距离的威胁评分

### 3.5 动态效果支持
```typescript
// 实时位置更新和动画效果
const positionsMap = new Map<string, { x: number; y: number }>();
processedExternalTargets.forEach(target => {
  positionsMap.set(target.id, target.position);
});
radarStore.setTargetDisplayPositions(positionsMap);
```

## 4. 交互设计与按键功能

### 4.1 键盘控制系统
```typescript
// TDC光标控制
useKeyboardControl({
  moveStep: 5,
  boundaries: {
    minX: framePositions.startX + padding,
    maxX: framePositions.endX - padding,
    minY: framePositions.startY + padding,
    maxY: framePositions.endY - padding
  },
  onKeyAction: handleTdcKeyAction
});
```

**核心按键功能**：

| 按键 | 功能 | 详细说明 |
|------|------|----------|
| **↑ ↓ ← →** | TDC光标移动 | 精确控制TDC位置，步长5像素 |
| **Enter** | 目标锁定确认 | 锁定TDC附近最近的目标（30像素阈值） |
| **IFF区域点击** | 敌我识别 | 点击IFF框触发目标识别和信息显示 |

### 4.2 鼠标交互系统
```typescript
// 目标点击交互
const handleTargetClickInIFF = useCallback((target: RadarTarget) => {
  if (!iffMode) return;
  
  // 显示目标详细信息
  onAddMessage('info', `目标ID: ${target.id}`);
  onAddMessage('info', `目标类型: ${target.type === 'army' ? '敌机' : '友机'}`);
  onAddMessage('info', `距离: ${targetInfo.distance.toFixed(2)} 海里`);
}, [iffMode, calculateTargetInfo]);
```

**鼠标操作功能**：
- **目标点击**：在IFF模式下点击目标获取详细信息
- **IFF框点击**：激活敌我识别模式和结果确认
- **按钮交互**：左右侧功能按钮的鼠标点击操作

### 4.3 按钮功能布局

**左侧按钮功能**：
```typescript
// 左侧按钮功能映射
left button 1: handleClearAndReset() // 清除重置
left button 2: // 天线高度调整
left button 3: 扫描角度切换 // 15°/30°/60°循环
left button 4: // 预留功能
left button 5: BR计数切换 // 1→2→4→1循环
```

**右侧按钮功能**：
```typescript
// 右侧按钮功能映射
right button 1: 雷达量程增加 // 10→20→40→80→10循环
right button 2: 雷达量程减少 // 80→40→20→10→80循环
right button 3: VectorHUD切换 // 显示/隐藏速度矢量
right button 4: 未知目标切换 // 显示/隐藏目标
right button 5: 显示模式切换 // AUTO→HI→MED→AUTO循环
right button 6: 静默模式切换 // 启用/禁用雷达静默
```

### 4.4 智能目标选择逻辑
```typescript
// Enter键触发的目标选择
const handleKeyDown = useCallback((e: KeyboardEvent) => {
  if (e.key === 'Enter' && processedExternalTargets && onTargetSelect) {
    // 查找TDC附近最近的目标
    let closestTarget: RadarTarget | undefined;
    let minDistance = 30; // 30像素阈值
    
    processedExternalTargets.forEach(target => {
      const distance = Math.sqrt(
        Math.pow(target.position.x - tdcPosition.x, 2) + 
        Math.pow(target.position.y - tdcPosition.y, 2)
      );
      
      if (distance < minDistance) {
        minDistance = distance;
        closestTarget = target;
      }
    });
    
    if (closestTarget) {
      onTargetSelect({
        targetId: closestTarget.id,
        lockX: tdcPosition.x,
        iffMode: iffMode
      });
    }
  }
}, [tdcPosition, processedExternalTargets]);
```

### 4.5 界面状态指示器

**雷达状态显示**：
```typescript
// 界面文本状态指示
TWS: 跟踪扫描模式
CNTL: 控制模式
SIL: 静默状态（红色表示激活）
STBY: 待机状态（带删除线表示非待机）
IFF: 敌我识别（带边框表示可点击）
```

**数值显示区域**：
- **扫描角度**：当前扫描扇角（15°/30°/60°）
- **雷达量程**：当前探测距离（10/20/40/80海里）
- **航向信息**：实时航向角度显示
- **BR计数**：扫描循环计数显示

### 4.6 用户体验优化
- **视觉反馈**：操作状态通过颜色、动画等方式实时反馈
- **信息层次**：重要信息优先显示，详细信息按需展示
- **操作容错**：提供操作确认和撤销机制
- **可访问性**：支持键盘和鼠标的双重操作方式
- **状态持久化**：关键操作状态在会话期间保持

## 总结

Radar.tsx的实现体现了现代雷达显示系统的专业性要求：
1. **高性能渲染**：Canvas技术确保流畅的实时显示
2. **精确坐标计算**：多坐标系转换保证位置信息的准确性
3. **直观符号系统**：清晰的目标标识和状态表示
4. **友好交互体验**：多模式输入和智能化操作辅助

这种设计既满足了军用雷达系统的专业要求，又提供了现代化的用户体验。